version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - API_KEY=your-secret-api-key
      - DEFAULT_PROJECT_ID=default
      - DATA_DIR=/app/data
    restart: unless-stopped